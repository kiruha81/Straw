<%= bootstrap_form_with(model: [shop, comment], local: false) do |f| %>
  <td><%= f.text_area :title, placeholder: "タイトルを記入" %></td>
  <td><%= f.text_area :comment, size: "48x2", placeholder: "コメントどうぞ" %></td>
  <td><%= f.submit "コメントを送信" %></td>
<% end %>