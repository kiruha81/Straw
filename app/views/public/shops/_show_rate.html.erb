<div class="starrate">
<%# 平均点を算出し、round関数で切り上げ %>
<%= @all_rating %>：<%= @reviews.average(:all_rating).to_f.round(1) %>
<%# 評価数を星に置き換える %>
<div class="average-review-rating" data-score=<%= @reviews.average(:all_rating) %>></div>
<%# jquery発火イベントを記載%>
<script>
  $('.average-review-rating').raty({
    readOnly: true,
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: function() {
      return $(this).attr('data-score')
    }
  });
</script>
<br>
<%# 同様に、rating1なども記載する%>
</div>

<div class="starrate">
<%# 平均点を算出し、round関数で切り上げ %>
<%= @rating1 %>：<%= @reviews.average(:rating1).to_f.round(1) %>
<%# 評価数を星に置き換える %>
<div class="review-rating1" data-score=<%= @reviews.average(:rating1) %>></div>
<%# jquery発火イベントを記載%>
<script>
  $('.review-rating1').raty({
    readOnly: true,
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: function() {
      return $(this).attr('data-score')
    }
  });
</script>
<br>
<%# 同様に、rating1なども記載する%>
</div>

<div class="starrate">
<%# 平均点を算出し、round関数で切り上げ %>
<%= @rating2 %>：<%= @reviews.average(:rating2).to_f.round(1) %>
<%# 評価数を星に置き換える %>
<div class="review-rating2" data-score=<%= @reviews.average(:rating2) %>></div>
<%# jquery発火イベントを記載%>
<script>
  $('.review-rating2').raty({
    readOnly: true,
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: function() {
      return $(this).attr('data-score')
    }
  });
</script>
<br>
<%# 同様に、rating1なども記載する%>
</div>

<div class="starrate">
<%# 平均点を算出し、round関数で切り上げ %>
<%= @rating3 %>：<%= @reviews.average(:rating3).to_f.round(1) %>
<%# 評価数を星に置き換える %>
<div class="review-rating3" data-score=<%= @reviews.average(:rating3) %>></div>
<%# jquery発火イベントを記載%>
<script>
  $('.review-rating3').raty({
    readOnly: true,
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: function() {
      return $(this).attr('data-score')
    }
  });
</script>
<br>
<%# 同様に、rating1なども記載する%>
</div>

<div class="starrate">
<%# 平均点を算出し、round関数で切り上げ %>
<%= @rating4 %>：<%= @reviews.average(:rating4).to_f.round(1) %>
<%# 評価数を星に置き換える %>
<div class="review-rating4" data-score=<%= @reviews.average(:rating4) %>></div>
<%# jquery発火イベントを記載%>
<script>
  $('.review-rating4').raty({
    readOnly: true,
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: function() {
      return $(this).attr('data-score')
    }
  });
</script>
<br>
<%# 同様に、rating1なども記載する%>
</div>